{
  "name": "autoscelem",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "node scripts/predeploy.js && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}
